enum Gender { male, female, others }

enum ChipLayoutType { row, column, wrap }

enum ChipSelectionType { single, multiple }

enum ChipFitBehavior { fill, fit }

//fill-> full width
//fit-> content size
